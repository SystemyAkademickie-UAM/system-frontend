/** Exit phase duration (ms) — synced with `--auth-card-transition-exit-ms`. */
export const AUTH_CARD_TRANSITION_EXIT_MS = 240;

/** Enter phase duration (ms) — synced with `--auth-card-transition-enter-ms`. */
export const AUTH_CARD_TRANSITION_ENTER_MS = 240;

/** Horizontal slide (% of card width); clipped by transition viewport. */
export const AUTH_CARD_TRANSITION_SLIDE_PERCENT = 50;
