/** Domyślny symbol waluty grupy (placeholder do czasu API konfiguracji waluty). */
export const DEFAULT_CURRENCY_SYMBOL = '🥕';

/** Domyślna nazwa pliku ikony waluty w public/assets/svg/ (gdy backend dostarczy URL/ID). */
export const DEFAULT_CURRENCY_ICON_FILE = null;
