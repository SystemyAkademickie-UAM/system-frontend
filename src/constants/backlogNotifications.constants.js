/** Jak często frontend sprawdza, czy doszło nowe powiadomienie (ms). Tylko frontend — backend nie musi tego obsługiwać. */
export const BACKLOG_NEW_NOTIFICATION_POLL_MS = 15_000;

/** Domyślny interwał odświeżania listy powiadomień w dzwonku i sekcjach (ms). */
export const BACKLOG_LIST_POLL_MS = 60_000;
