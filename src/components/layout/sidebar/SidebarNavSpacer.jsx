import '../navigation-shell.css';

/** Pionowa przerwa na pasku nawigacji (połowa wysokości przycisku). */
export default function SidebarNavSpacer() {
  return <li className="sidebar-nav__spacer" aria-hidden="true" />;
}
