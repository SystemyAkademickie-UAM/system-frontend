import { PageHeader } from '../../../../components/ui/index.js';

export default function GroupSettingsCurrencyPage() {
  return (
    <section aria-labelledby="group-settings-currency-title">
      <PageHeader
        title="Waluta"
        description="Konfiguruj walutę grupową."
      />
      <p style={{ color: 'var(--color-text-secondary)', padding: '1rem' }}>
        Strona w budowie — ustawienia waluty.
      </p>
    </section>
  );
}
