# API base URL for browser requests (no trailing slash).
# Leave unset in local dev to default to http://127.0.0.1:8080.
# For `docker build`, pass the URL the user's browser will use, e.g.:
#   docker build --build-arg VITE_API_BASE_URL=http://127.0.0.1:8080 .
# VITE_API_BASE_URL=
